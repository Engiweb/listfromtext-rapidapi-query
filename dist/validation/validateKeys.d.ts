import { listfromtextApi, listfromtextResponse } from '../../@types/types';
declare const validateKeys: ({ endpoint, apiKey, input, file, unique, sort, search, }: listfromtextApi) => listfromtextResponse;
export default validateKeys;
